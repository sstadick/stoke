
[workspace]
authors = ["Seth Stadick <sstadick@gmail.com>"]
channels = [
    "https://prefix.dev/conda-forge",
    "https://conda.modular.com/max-nightly",
    "https://repo.prefix.dev/modular-community",
]
platforms = ["linux-64", "osx-arm64"]
preview = ["pixi-build"]

[package]
name = "mojopt"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-mojo", version = "0.*", channels = [
    "https://prefix.dev/pixi-build-backends",
    "https://prefix.dev/conda-forge",
    "https://repo.prefix.dev/modular-community",
] }

[package.host-dependencies]
mojo-compiler = "0.*"

[package.build-dependencies]
mojo-compiler = "0.*"

[package.run-dependencies]
mojo-compiler = "0.*"

[tasks]
r = "mojo run main.mojo"
t = { cmd = "sh -c 'find ./tests -name test_*.mojo | xargs -I % mojo run -I . -D ASSERT=all %'" }
examples = { cmd = "sh -c 'find ./examples -name *.mojo | xargs -I % mojo run -I . -D ASSERT=all % --help'" }
lint = { cmd = "mojo format --line-length 100 mojopt/* tests/* examples/*" }

[dependencies]
mojo = "==0.26.2"
# mojopt = { path = "." }
